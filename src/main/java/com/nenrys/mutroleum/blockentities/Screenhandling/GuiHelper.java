package com.nenrys.mutroleum.blockentities.Screenhandling;

import net.minecraft.entity.player.PlayerEntity;
import net.minecraft.entity.player.PlayerInventory;
import net.minecraft.screen.ScreenHandler;
import net.minecraft.screen.ScreenHandlerType;
import net.minecraft.screen.slot.Slot;
import org.jetbrains.annotations.Nullable;

public class GuiHelper {

    /*
    public void addPlayerInv(PlayerInventory playerinventory, ScreenHandler handler) {
        int m;
        int l;

        for(m=0; m< 3; ++m) {
            for(l=0; l<9; ++l) {
                handler.addSlot(new Slot(playerinventory, l+m*9+9, 8+l*18, 84+m*18));
            }
        }

        for(m=0; m<9; ++m) {
            handler.addSlot(new Slot(playerinventory, m, 8+m*18, 142));
        }
        
        this.addSlot(new Slot(playerinventory, m, 8+m*18, 142))
    }

     */
}
